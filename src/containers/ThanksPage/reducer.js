import _ from 'lodash';



// The initial state of the App
const initialState = {
};

function thanksPageReducer(state = initialState, action) {
  switch (action.type) {
   
    default:
      return state;
  }
}

export default thanksPageReducer;
